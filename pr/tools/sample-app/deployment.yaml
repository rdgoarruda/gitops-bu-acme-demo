apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app
  namespace: bu-acme-workloads
  labels:
    app: sample-app
    bu: bu-acme
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sample-app
  template:
    metadata:
      labels:
        app: sample-app
        bu: bu-acme
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          envFrom:
            - configMapRef:
                name: app-config
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
      volumes:
        - name: html
          configMap:
            name: app-html
            items:
              - key: index.html
                path: index.html
